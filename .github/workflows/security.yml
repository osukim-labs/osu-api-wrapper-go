name: Security

on:
    push:
        branches:
            - master
            - main
    pull_request:
    
jobs:
  Gosec:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Repository
        uses: actions/checkout@v6
      - name: Run Gosec
        uses: securego/gosec@master
        with:
          args: ./...